// implemented later

    /// Current state of a single build task.
    pub enum TaskStatus {}

    /// State of the overall build graph.
    pub struct BuildState;

    /// Top-level daemon state.
    pub struct DaemonState;
    